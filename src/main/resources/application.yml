##web服务配置
server: 
  port: 8080
  ##配置默认首页地址
  servlet:
    context-path: /
    
spring:
#数据源配置
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_book
    password: 11
    username: root
    
#JPA配置
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true